These exist

panel.removeAll() - clear so that you can reset it
view.getContentPane().removeAll();
thing.setMaximumSize(new Dimension(fullWidth, rowHeight))
rowWrapper.setBorder(new EmptyBorder(5, 5, 5, 30)); // Add margins around each rowWrapper

for resetting
panelCenter.revalidate();
panelCenter.repaint();